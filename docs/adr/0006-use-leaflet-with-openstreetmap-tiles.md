# Use Leaflet with OpenStreetMap tiles

The public site uses Leaflet with OpenStreetMap tiles for the zoomable world map. This keeps the personal static site free of map API keys and billing setup while still supporting markers, marker spreading, smooth pan/zoom, and synchronized photo selection.
