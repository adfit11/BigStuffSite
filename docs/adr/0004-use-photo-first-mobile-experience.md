# Use a stacked mobile map and photo experience

The public site uses different primary interactions on desktop and mobile. Desktop emphasizes the zoomable map with a synchronized title list and feature panel, while mobile stacks the map above the photo viewer and omits the title list, relying on fast swipe-based navigation and marker selection instead of forcing the desktop scroll-centered list interaction onto a small screen.
