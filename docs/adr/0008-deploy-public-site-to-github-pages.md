# Deploy public site to GitHub Pages

The public site targets GitHub Pages as a static deployment host. This fits the site's generated-data architecture, avoids a backend server, and lets updates publish by committing regenerated public assets and site files to the repository.
