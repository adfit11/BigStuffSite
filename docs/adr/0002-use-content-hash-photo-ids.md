# Use content-hash photo IDs

Photo entries use an ID derived from the image file's content rather than the filename. This keeps owner-authored descriptions and tags attached to the same photo even when files are renamed before or after import.
