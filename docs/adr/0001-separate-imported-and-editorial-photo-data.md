# Separate imported and editorial photo data

The site keeps automatically imported photo facts separate from owner-authored editorial metadata. Generated import data can be rebuilt from downloaded image files, while descriptions and tags live in a separate editable file so routine imports do not overwrite hand-written context.
