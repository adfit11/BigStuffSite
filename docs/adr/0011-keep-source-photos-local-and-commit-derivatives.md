# Keep source photos local and commit derivatives

Source photos live in a git-ignored local folder and are not published. The generated public photo derivatives are committed to the repository for the initial version so GitHub Pages can deploy the static site without needing image processing in CI.
