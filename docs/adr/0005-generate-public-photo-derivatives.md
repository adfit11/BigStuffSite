# Generate public photo derivatives

The importer treats downloaded originals as source inputs and generates public web-sized images and thumbnails for the site to display. This keeps the public site fast, avoids serving full-resolution originals, and lets import metadata be regenerated without depending on visitor-facing image paths being the original files.
