# Domain Docs

This repo uses a single domain context.

## Before exploring, read these

- `CONTEXT.md` at the repo root
- ADRs in `docs/adr/` that touch the area being changed

Use the glossary's vocabulary in issues, PRDs, implementation notes, and tests. If an implementation decision contradicts an ADR, surface the conflict explicitly.
